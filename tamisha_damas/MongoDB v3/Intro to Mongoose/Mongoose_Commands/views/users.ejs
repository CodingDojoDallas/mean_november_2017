<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Basic Mongoose</title>
  </head>
  <body>
    <h4>User Information:</h4>
    <% for(var x in users) { %>

      <p>User: <%= users[x].first_name %> <%= users[x].last_name %></p>
      <p>Age: <%= users[x].age %></p>
      <p>Email: <%= users[x].email %></p>
      <hr>

    <% } %>
    <a href="/"><h3>Back</h3></a>
  </body>
</html>

<% for(var x in users) { %>

  <p>User: <%= users[x].first_name %> <%= users[x].last_name %></p>
  <p><%= users[x].age %></p>
  <p><%= users[x].email %></p>

<% } %>
